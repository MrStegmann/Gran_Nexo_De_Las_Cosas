// Este archivo será la ventana principal que cargará y montará todos los components
import React from "react";

export const TemplateFeature: React.FC = () => {
  return (
    <div className="w-full h-full">
      <p>Template</p>
    </div>
  );
};